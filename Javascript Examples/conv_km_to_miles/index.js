//<=================Convert Km To Miles=========================>

function convKmToMiles(num) {
	let factor = 0.621371;
	let kmTomiles = num * factor;

	console.log(kmTomiles);
}

convKmToMiles(30);
